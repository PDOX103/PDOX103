# Reference video analysis and applied result

The supplied screen recording shows a mobile view of the reference GitHub profile. Its banner uses one terminal-style window with a stable `SYSTEM.INFO` panel and an animated `VISUAL.MAP` panel.

## Reference sequence

1. Dithered head-and-shoulders portrait
2. First technology/logo shape
3. Second technology/logo shape
4. Third technology/logo shape
5. Return to the portrait

The red `LIVE` indicator pulses throughout, while the information panel and handle pill remain visible.

## Applied to PDOX103

The final `dark.svg` and `light.svg` keep that animation architecture but use the PDOX103 portrait and profile information. The files include the portrait shimmer, dense portrait dissolve, traveller morph paths, terminal layout, dotted information rows, and theme-specific colors.
