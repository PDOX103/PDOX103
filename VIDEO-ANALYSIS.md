# Reference video analysis

The supplied screen recording is 25.4 seconds long, 462×850 pixels, and 30 fps. It shows a mobile view of the `arifhaxn` GitHub profile.

## Visual sequence

1. A dark terminal-style banner fills the top of the profile.
2. The left `VISUAL.MAP` panel cycles through four states:
   - Dithered head-and-shoulders portrait
   - `</>` code symbol
   - Brain-like technology symbol
   - Triangle-shaped symbol
3. The right `SYSTEM.INFO` panel remains stable while the visual state changes.
4. A red `LIVE` indicator pulses and a coloured handle pill remains visible.
5. Below the banner, the profile displays:
   - Full-width contribution streak card
   - Two statistics cards side by side
   - Contribution snake
   - Animated two-column project cards
   - Social badges

## Applied to PDOX103

The updated package follows that same order, palette, layout and automation model. All `arifhaxn` identity fields and URLs were removed and replaced with the details from the supplied `PDOX103` profile archive.

The reference person's portrait was not copied. The included banner uses an abstract dithered head-and-shoulders placeholder. The supplied Python generator can replace it with the real PDOX103 portrait once a suitable photograph is provided.
